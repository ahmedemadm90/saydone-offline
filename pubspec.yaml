name: saydone_offline
description: Fully offline voice-to-task AI assistant.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sqflite: ^2.3.3+3
  path: ^1.9.0
  path_provider: ^2.1.2
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  record: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
